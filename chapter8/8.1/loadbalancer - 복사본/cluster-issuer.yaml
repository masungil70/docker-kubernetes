apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    # ACME 서버 주소 (프로덕션용)
    server: https://acme-v02.api.letsencrypt.org/directory
    # Let's Encrypt 계정으로 사용할 이메일 주소
    # 반드시 실제 사용하는 이메일로 변경해주세요. (인증서 만료 알림 등에 사용)
    email: masungil2@daum.net
    # ACME 계정의 개인키를 저장할 Secret 리소스의 이름
    privateKeySecretRef:
      name: letsencrypt-prod-private-key
    # 도메인 소유자임을 증명하는 방법(Challenge) 설정
    solvers:
    - http01:
        ingress:
          # Ingress Controller의 종류를 지정합니다.
          # AWS Load Balancer Controller를 사용하므로 class를 alb로 지정합니다.
          class: alb
