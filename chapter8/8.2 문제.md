1. k8s-master1 1대와 k8s-worker1, k8s-worker2를 생성하고 고정 아이피를 설정하고 쿠버네티스를 클러스터로 구성하세요 

2. 쿠버네티스 구문 자동완성 기능을 사용할 수 있게 설정하세요 

3. pod 컨테이너 이미지로 nginx를 설정하고 
   pod 라벨은 nginx-app으로 설정하세요 
   실행시 복제 본수는 3개로 해서 Deployment로 실행 할 수 있는 yaml 파일을 생성 합니다 
   
   pod 클러스터 아이피를 확인하고 pod 클러스터 아이피로 nginx 웹 서버의 결과를 확인합니다 
   
   pod로 실행되는 nginx을 윈도우에서 실행을 하려고 합니다 
   어떻게 하면 되는지 생각해 보세요 
   

4. 위 파드를 Node Port로 실행 할 수 있게 서비스를 생성해주세요 
   k8s-master1, k8s-worker1, k8s-worker2의 ip를 확인하고 port 를 확인 한 후 node:port로 실행해 봅니다 

5. metalLB을 설치해주세요 

6. 4번를 type을 LoadBalancer 로 변경하고 서비스의 목록을 확인합니다 
   외부에서 접근 할 수 있는 ip를 대역이 출력되는지 확인합니다 
   
7. 윈도우에서 외부에서 접근 할 수 있는 ip를 사용하여 앞의 nginx 웹 서버 실행결과를 확인한다 

8. 8장의 예제 apple/banana를 인그레스 예제를 다시 한번 리뷰 합니다 
   1차 인그레스 서비스의 type을 nodePort해서 실행을 합니다 
   2차 인그레스 서비스의 type을 LB로 변경해서 실행을 합니다 
   각각의 차이점을 정확히 확인합니다 
